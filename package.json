{
  "name": "@cloudwell/simple-theme",
  "version": "1.0.0",
  "description": "A theme provider that supports utilizing the system theme and a storage interface for allowing overrides.",
  "main": "index.js",
  "readme": "README.md",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc && cp package.json README.md ./dist",
    "deploy": "npm run build && npm publish ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Cloudwell-IO/simple-theme.git"
  },
  "author": "Cloudwell",
  "license": "GPL-3.0",
  "bugs": {
    "url": "https://github.com/Cloudwell-IO/simple-theme/issues"
  },
  "homepage": "https://github.com/Cloudwell-IO/simple-theme#readme",
  "peerDependencies": {
    "@types/react": "^16.8.0",
    "react": "^16.8.0",
    "react-dom": "^16.8.0",
    "typescript": "^4.6.4"
  }
}
